import React from 'react';

import { styles } from '../styles';

const Home = () => {
    return (
        <div style={styles.HomeContainer}>
            <h1 style={styles.HomeFont}>Jokes</h1>
        </div>
    );
};

export default Home;